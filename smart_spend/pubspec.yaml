name: novapay
description: A GCash-inspired full-stack spending tracker.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  intl: ^0.18.1
  google_fonts: ^6.1.0
  mobile_scanner: ^7.2.0
  qr_flutter: ^4.1.0
  local_auth: ^3.0.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
